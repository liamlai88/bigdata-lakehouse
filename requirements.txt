# 阶段 1 依赖
pandas>=2.2
pyarrow>=17.0
faker>=25.0
boto3>=1.34
s3fs>=2024.6
tqdm>=4.66

# 阶段 2 依赖：Iceberg
pyiceberg[s3fs,pyarrow]>=0.7.0

# 阶段 4 依赖：Trino python 客户端
trino>=0.330

# 阶段 5 依赖：Kafka 客户端 (给 producer 用)
confluent-kafka>=2.5

# 阶段 7 依赖：百炼 (NL2SQL)
dashscope>=1.20
